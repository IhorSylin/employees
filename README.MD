# Usage

## To run backend

`cd employees-api`
`docker-compose build`
`docker-compose up`
`docker exec -it employees-app npm run db:setup`

## To run frontend

`cd employees-ui`
`npm install`
`npm run dev`
